<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet" />
    <style>
      body {
        font-family: 'Poppins', sans-serif;
      }
    </style>
  </head>
  <body class="min-h-screen flex items-center justify-center bg-gradient-to-r from-[#0f2027] via-[#203a43] to-[#2c5364] p-5">
    <main>
      <form action="/login" method="POST" class="w-full max-w-md p-8 bg-white/10 backdrop-blur-md border border-white/20 rounded-2xl shadow-2xl text-center hover:scale-105 transition-transform duration-300">
        <h2 class="text-2xl font-bold text-white mb-6">Login</h2>

        <label class="block text-white text-left mb-1">Role:</label>
        <select name="role" required class="w-full p-3 mb-4 rounded-md bg-white/90 focus:outline-none focus:ring-2 focus:ring-cyan-400">
          <option value="user">User</option>
          <option value="vendor">Vendor</option>
        </select>

        <label class="block text-white text-left mb-1">Email:</label>
        <input type="email" name="email" required class="w-full p-3 mb-4 rounded-md bg-white/90 focus:outline-none focus:ring-2 focus:ring-cyan-400" />

        <label class="block text-white text-left mb-1">Password:</label>
        <input type="password" name="password" required class="w-full p-3 mb-4 rounded-md bg-white/90 focus:outline-none focus:ring-2 focus:ring-cyan-400" />

        <button type="submit" class="w-full py-3 mt-2 rounded-md bg-gradient-to-r from-cyan-400 to-blue-600 text-white font-semibold hover:from-blue-600 hover:to-cyan-400 transition-all duration-300">Login</button>

        <p class="text-gray-200 mt-5 text-sm">
          Don't have an account? <a href="/signup" class="text-cyan-300 hover:underline">Signup</a>
        </p>
      </form>
    </main>
  </body>
</html>
