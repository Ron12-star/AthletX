<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Order Pending</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <main class="container">
    <h2>✅ Order Successfully Placed!</h2>

    <% const safeOrder = order || {}; %>

    <p><strong>Name:</strong> <%= safeOrder.name || 'N/A' %></p>
    <p><strong>Address:</strong> <%= safeOrder.address || 'N/A' %></p>
    <p><strong>Total Amount:</strong> ₹<%= safeOrder.totalAmount || '0' %></p>
    <p><strong>Payment Method:</strong> <%= safeOrder.paymentMethod || 'N/A' %></p>

    <a href="/">⬅️ Back to Home</a>
  </main>
</body>
</html>
